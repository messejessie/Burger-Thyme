<h1>Burgers!</h1>

<h2>Burger Graveyard!</h2>

<ul>
    {{#each Burgers}}
    {{#unless yummy}}
    {{> Burgers/Burger-block yummy=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that are yummy!</h2>

<ul>
    {{#each Burgers}}
    {{#if yummy}}
    {{> Burgers/Burger-block yummy=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
        <label for="yummy">yummy?</label><br>
        <input type="radio" name="yummy" value="1" checked> yummy!<br>
        <input type="radio" name="yummy" value="0"> Hungry
    </div>

    <button type="submit">Add Burger</button>
</form>